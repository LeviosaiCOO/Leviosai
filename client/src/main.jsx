import React from "react";
import ReactDOM from "react-dom/client";
import CatalystApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CatalystApp />
  </React.StrictMode>
);
